# 小说生成器需求文档

## 1. 项目背景
开发一个小说生成系统，通过接入 AI 能力批量生成小说内容，内置自动审核机制确保内容符合平台规范，在关键节点让用户把控方向，最终输出可直接发布的小说。

## 2. 核心需求

### 2.1 AI 批量生成
- 接入 AI 模型（如 Kimi、OpenAI 等）自动生成小说内容
- 按标准网络小说写作流程分阶段生成
- 支持批量生成章节内容

### 2.2 写作流程标准化
基于网络小说创作的标准流程：
1. **题材确定** - 类型、卖点、目标读者
2. **世界背景** - 世界观、规则、势力设定
3. **人物设定** - 主角、配角性格、能力、关系
4. **主线大纲** - 核心起承转合、高潮设计
5. **章纲细化** - 每章标题+一句话概括
6. **开篇生成** - 前3章内容（黄金三章）
7. **批量生成** - 按章纲批量生成正文
8. **质量检查** - 一致性、逻辑性检查

### 2.3 关键节点人工审核
仅在以下关键节点需要用户审核确认：
- **大纲阶段** - 确认故事走向是否符合预期
- **开篇阶段** - 确认文风、人物设定是否满意
- **知识变更阶段** - 确认每章后的知识变更（人物等级、物品获得等）
- **最终导出** - 确认整体内容可发布

其他阶段由系统自动完成，无需人工干预。

### 2.4 风格选择
- 支持多种小说类型风格（玄幻、仙侠、都市、科幻等）
- 每种风格有对应的 Prompt 模板
- 用户可选择目标发布平台（番茄、起点等），系统自动适配对应风格

### 2.5 自动平台合规检查
**核心功能**：系统自动检测并修改不符合平台要求的内容

- **检测内容**：
  - 敏感词检测（涉政、色情、暴力、犯罪等）
  - 语义级风险分析（上下文理解）
  - 平台规则适配（番茄/起点等不同规则）

- **自动修复**：
  - 自动替换敏感词为合规表达
  - 自动淡化过度描写
  - 自动调整不符合平台调性的内容
  - 对无法自动修复的内容标记提示用户

- **检查时机**：
  - 每章生成后自动检查
  - 批量生成过程中持续监控
  - 最终导出前全面检查

### 2.6 小说知识库（新增）
**核心功能**：维护小说设定的一致性，防止生成过程中出现混乱

- **知识库内容**：
  - 世界观设定（世界规则、修炼体系、势力分布）
  - 人物档案（等级、物品、功法、关系）
  - 物品档案（归属、能力、获得历程）
  - 功法档案（等级、效果、修炼进度）
  - 时间线（大事记、人物成长、物品获得）

- **分层Prompt策略**（解决Token限制）：
  - **第一层（核心知识，必注入）**：本章出场人物状态、关键物品
  - **第二层（相关上下文，选择性注入）**：前3章摘要、相关配角
  - **第三层（背景知识，按需注入）**：世界规则、历史事件

- **智能摘要**：
  - 为每个人物/物品生成动态摘要
  - 摘要优先用于Prompt注入
  - 完整信息在需要时检索

### 2.7 知识确认机制（新增）
**核心功能**：人机结合确保知识库准确性

- **每章生成后**：
  1. 系统自动提取知识变更草稿
  2. 展示变更摘要给用户（如"林凡: 炼体九层 → 筑基一层"）
  3. 用户一键确认或修改
  4. 确认后更新知识库

- **自动确认规则**：
  - 明显的数值变化（如丹药数量-1）可自动确认
  - 重要变化（等级突破、获得新物品）必须人工确认

### 2.8 一致性检查（新增）
**核心功能**：确保生成内容与知识库一致

- **轻量级检查（每章必做）**：
  - 基于规则的关键词匹配
  - 检查人物等级、物品归属等
  - 速度快（毫秒级），零成本

- **重级检查（抽查）**：
  - 基于LLM的深度语义检查
  - 每10章抽查1章
  - 用户标记可疑章节时检查
  - 最终导出前全面检查

### 2.9 知识库版本控制（新增）
**核心功能**：支持知识修正和重新生成

- **版本控制**：
  - 每次知识变更自动保存版本
  - 可查看历史变更记录
  - 支持回滚到任意版本

- **批量修正**：
  - 修正某知识后，可选择重新生成相关章节
  - 自动标记受影响的章节

- **用户标记重要知识**：
  - 用户可标记某些知识为"重要"
  - 重要知识在Prompt中优先注入
  - 重要知识变更时需要额外确认

### 2.10 界面风格需求（新增）
**核心需求**：暗黑电竞风（Dark Gaming）界面，炫酷、年轻化、沉浸式体验

- **设计主题**：「末世生存者终端」- 为玄幻/灵异/末世题材打造的沉浸式创作界面

- **配色方案**：
  - 主背景：深邃黑 `#0a0a0f`
  - 次级背景：`#12121a`、`#1a1a25`
  - 主强调色：烈焰橙 `#ff4d00`（发光效果）
  - 次强调色：电光蓝 `#00d4ff`（发光效果）
  - 成功状态：毒液绿 `#00ff88`
  - 警告/错误：血红色 `#ff0040`
  - 文字：纯白 `#ffffff`、灰白 `#a0a0b0`

- **视觉特效**：
  - 动态粒子背景（能量流动效果）
  - 扫描线效果（复古CRT显示器）
  - 霓虹发光边框（所有卡片和按钮）
  - 血条式进度条（带流动光效）
  - 打字机文字生成效果

- **游戏化UI元素**：
  - 人物等级像游戏角色一样展示（Lv.XX + 经验条）
  - 物品像游戏装备一样展示（品质颜色、属性面板）
  - 生成进度用能量条/血条展示
  - 系统日志像游戏控制台
  - 知识确认像系统警报弹窗

- **动画效果**：
  - 按钮悬停发光+上浮效果
  - 进度条流动光效
  - 文字逐字打字机效果
  - 警报边框闪烁效果

### 2.11 AI 接口配置（新增）
**核心功能**：支持多 AI 提供商的灵活配置与管理

- **多提供商支持**：
  - 支持 Kimi、OpenAI、DeepSeek 等主流 AI 提供商
  - 支持自定义 OpenAI 兼容接口（填写 Base URL）
  - 可同时配置多个模型，设置优先级和 fallback 顺序

- **配置项**：
  - API Key（加密存储，界面脱敏显示）
  - Base URL（API 接口地址）
  - 模型名称（如 moonshot-v1-128k、gpt-4-turbo）
  - 温度（Temperature）、最大 Token 数
  - 启用/禁用开关

- **连通性测试**：
  - 一键测试 API 是否可用
  - 显示测试结果（成功/失败/耗时）
  - 记录最后一次测试时间和状态

- **Fallback 策略**：
  - 当前模型不可用时自动切换到下一优先级模型
  - 指数退避重试（最大 3 次）
  - 重试间隔可配置

### 2.12 多端支持（新增）
**核心功能**：支持多种终端访问，确保跨设备体验

- **Electron 桌面应用**：
  - 打包为 Windows .exe 安装包
  - 粒子背景、扫描线、发光等视觉特效全开不降级
  - 桌面专属功能：自定义标题栏、系统托盘、原生通知、全局快捷键

- **Web 桌面浏览器**：
  - 通过浏览器直接访问，功能完整
  - 视觉特效全开（与 Electron 桌面端一致）

- **Web 手机浏览器**：
  - 响应式布局，适配手机屏幕（375px - 767px 单列布局）
  - 触摸友好的交互（所有可点击元素 ≥ 44px）
  - 知识库以全屏抽屉形式展示
  - 底部固定操作栏（暂停/确认/知识库）
  - 视觉特效合理降级（粒子减少、扫描线禁用、发光减弱）

- **架构说明**：
  - 三端共用同一套前端代码（React）
  - 所有客户端连接同一个云端后端
  - 通过环境检测自动适配特效级别

### 2.13 用户认证（新增）
**核心功能**：云端部署下的访问安全

- 用户登录/登出
- 密码保护（bcrypt 哈希存储）
- JWT Token 认证
- 首次部署创建管理员账号

## 3. 用户场景

### 场景 1：快速生成小说
1. 用户登录系统
2. 首次使用时配置 AI 接口（API Key、模型选择）
3. 输入书名、选择类型（如玄幻）、目标平台（如番茄）
4. 系统自动生成大纲 → 暂停等待用户确认
5. 用户确认或提出修改意见
6. 系统生成开篇3章 → 暂停等待用户确认
7. 用户确认后，系统自动批量生成剩余章节
   - 每章生成后自动提取知识变更
   - 展示变更摘要，用户一键确认
   - 自动进行合规检查和一致性检查
8. 全部生成完毕，用户导出小说文件

### 场景 2：修改已有内容
1. 用户发现某章节人物等级错误
2. 在知识库中修正该人物等级
3. 系统标记受影响的后续章节
4. 用户选择重新生成相关章节
5. 系统自动使用修正后的知识重新生成

### 场景 3：查看知识库
1. 用户点击知识库抽屉
2. 查看人物、物品、功法等统计
3. 点击具体人物查看详细信息
4. 查看时间线了解故事发展

### 场景 4：手机端查看进度
1. 用户在手机浏览器登录系统
2. 查看当前批量生成进度
3. 确认知识变更摘要
4. 查看已生成章节内容

## 4. 功能列表

### 4.1 必需功能（MVP）
- [ ] 用户登录认证（JWT）
- [ ] AI 接口配置（多提供商、API Key、连通性测试）
- [ ] 项目创建与管理（书名、类型、目标平台）
- [ ] 按流程生成（大纲→开篇→批量）
- [ ] 关键节点暂停等待用户确认
- [ ] **分层Prompt策略**（核心知识+相关上下文）
- [ ] **知识变更确认**（每章后展示摘要）
- [ ] 基础敏感词过滤
- [ ] 自动修复常见违规内容
- [ ] 移动端响应式适配
- [ ] 导出小说（TXT/Word）

### 4.2 增强功能
- [ ] **轻量级一致性检查**（规则匹配）
- [ ] **知识库版本控制**（历史记录、回滚）
- [ ] **批量修正与重新生成**
- [ ] Electron 桌面客户端（Windows .exe）
- [ ] 语义级合规分析（不仅是关键词）
- [ ] 多平台规则适配（番茄/起点/晋江）
- [ ] 批量生成进度实时监控
- [ ] 风格模板自定义
- [ ] 章节编辑功能
- [ ] 导出更多格式（PDF/ePub）

### 4.3 高级功能
- [ ] **重级一致性检查**（LLM抽查）
- [ ] **智能摘要**（动态生成人物/物品摘要）
- [ ] **用户标记重要知识**
- [ ] Electron 自动更新
- [ ] 人物关系图谱
- [ ] 时间线可视化

---

## 5. 非功能需求

### 5.1 性能
- 单章生成时间 < 30 秒
- 合规检查时间 < 5 秒/章
- **轻量级一致性检查 < 100毫秒/章**
- 支持后台批量生成
- Electron 桌面端粒子/扫描线/发光特效全开流畅运行

### 5.2 易用性
- 单页操作，无需跳转
- **抽屉式知识库**（需要时展开）
- 清晰的生成进度显示
- 一键修复违规内容
- 一键确认知识变更
- 手机浏览器操作流畅
- Electron 桌面端开箱即用

### 5.3 可靠性
- 生成失败可重试
- 自动保存进度
- 支持断点续生成
- **知识库版本可回滚**
- SSE 连接断开后自动重连恢复

## 6. 技术约束
- 前端：React + TailwindCSS + shadcn/ui，响应式适配手机浏览器
- 桌面端：Electron（打包 Windows .exe）
- 后端：Python + FastAPI
- AI：支持 Kimi / OpenAI / DeepSeek 等，通过配置界面管理
- 数据库：SQLite（WAL 模式）
- 部署：云端部署（自有服务器 / WSL），Docker Compose + Nginx + HTTPS
- 移动端：响应式 Web，兼容 Android Chrome / iOS Safari
