# NovelGenerator 验证报告

> **验证日期**: 2025-03-27  
> **验证环境**: Ubuntu Linux / Python 3.13 / Node.js v20.20.2  
> **验证范围**: Phase 0 (基础设施) + Phase 1 (MVP 核心功能)

---

## 1. 代码评审发现与修复

### 1.1 已发现并修复的问题

| # | 严重程度   | 文件                           | 问题描述                                                                 | 修复方式                                            |
|---|-----------|-------------------------------|-------------------------------------------------------------------------|---------------------------------------------------|
| 1 | **严重** | `backend/app/config.py`       | `JWT_SECRET` 使用 `os.urandom(32).hex()` 作为默认值，每次重启生成新密钥，导致所有已签发的 JWT 失效 | 改为稳定默认值 + UserWarning 提示生产环境必须设置 `.env` |
| 2 | **严重** | `backend/app/routers/projects.py` | 创建项目时，`WorkflowStateRecord` 引用 `project.id` 但此时 `project` 尚未 flush，id 为 None，导致 NOT NULL 约束失败 | 在创建 workflow state 前添加 `await db.flush()` |
| 3 | **高** | `backend/app/generation.py`   | `_update_workflow_state()` 仅执行 UPDATE，若记录不存在则静默失败                 | 改为 upsert 模式：先查询，不存在则创建 |
| 4 | **高** | `backend/app/routers/knowledge.py` | `/versions` 路由被 `/{entity_id}` 参数路由优先匹配，导致访问 `/versions` 返回 404 或错误 | 将 `/versions` 路由移至 `/{entity_id}` 之前 |
| 5 | **高** | `backend/requirements.txt`    | 缺少 `greenlet` 依赖，SQLAlchemy async 在 Python 3.13 上启动失败              | 添加 `greenlet>=3.0.0` |
| 6 | **高** | `backend/requirements.txt`    | `bcrypt 5.0.0` 移除了 `__about__` 模块，与 `passlib 1.7.4` 不兼容，导致注册/登录 500 错误 | 固定 `bcrypt==4.0.1` |

### 1.2 轻微问题（未修复，不影响功能）

| 问题 | 影响 |
|------|------|
| `react-router-dom` 已安装但未使用 | 增加约 20KB bundle 体积，无功能影响 |
| `encryption.py` 的加密密钥在未配置时自动生成，重启后旧数据无法解密 | 仅影响 AI API Key 的加密存储，开发环境可接受 |

---

## 2. API 接口测试

### 2.1 单元测试（test_api.py）

共 **26** 个测试用例，覆盖所有核心 API 端点。

| 测试分类           | 测试用例数 | 通过 | 覆盖内容                                |
|-------------------|-----------|------|----------------------------------------|
| 健康检查           | 1         | ✅ 1  | `GET /api/health`                       |
| 认证（注册/登录）   | 7         | ✅ 7  | setup, login, logout, me, 鉴权拒绝       |
| AI 配置管理        | 3         | ✅ 3  | 创建、列表、更新                          |
| 项目 CRUD         | 4         | ✅ 4  | 创建、列表、详情、更新                     |
| 章节列表           | 1         | ✅ 1  | 空列表返回                               |
| 生成状态           | 1         | ✅ 1  | workflow state 查询                      |
| 知识库             | 2         | ✅ 2  | 实体列表、版本列表                        |
| 大纲确认           | 1         | ✅ 1  | outline confirm + 状态变更               |
| 导出               | 1         | ✅ 1  | 无章节时返回 400                          |
| 输入校验           | 2         | ✅ 2  | 空标题 422、短密码 422                    |
| 404 处理           | 2         | ✅ 2  | 不存在的项目/章节                         |
| 删除               | 1         | ✅ 1  | 创建→删除→验证 404                        |

**结果: 26/26 全部通过 ✅**

### 2.2 完整 API 路由清单

```
POST   /api/auth/setup              — 首次管理员注册
POST   /api/auth/login              — 用户登录
POST   /api/auth/logout             — 用户登出
GET    /api/auth/me                 — 当前用户信息

GET    /api/settings/ai             — AI 配置列表
POST   /api/settings/ai             — 创建 AI 配置
PUT    /api/settings/ai/:id         — 更新 AI 配置
DELETE /api/settings/ai/:id         — 删除 AI 配置
POST   /api/settings/ai/:id/test    — 测试 AI 连接

GET    /api/projects                — 项目列表
POST   /api/projects                — 创建项目
GET    /api/projects/:id            — 项目详情
PUT    /api/projects/:id            — 更新项目
DELETE /api/projects/:id            — 删除项目

GET    /api/projects/:id/chapters   — 章节列表
GET    /api/chapters/:id            — 章节详情
PUT    /api/chapters/:id            — 更新章节
DELETE /api/chapters/:id            — 删除章节

POST   /api/projects/:id/generate/outline        — 生成大纲 (SSE)
POST   /api/projects/:id/generate/outline/confirm — 确认大纲
POST   /api/projects/:id/generate/opening         — 生成开篇 (SSE)
POST   /api/projects/:id/generate/batch           — 批量生成 (SSE)
POST   /api/projects/:id/generate/chapter/:no     — 重新生成 (SSE)
GET    /api/projects/:id/generate/status           — 工作流状态
GET    /api/projects/:id/generate/export/txt       — 导出 TXT

GET    /api/projects/:id/knowledge            — 知识实体列表
GET    /api/projects/:id/knowledge/versions   — 知识版本列表
GET    /api/projects/:id/knowledge/:entity_id — 实体详情
PUT    /api/projects/:id/knowledge/:entity_id — 更新实体
POST   /api/projects/:id/knowledge/confirm    — 确认知识变更
```

**共 31 个业务路由 + 4 个文档路由 (docs/redoc/openapi.json)**

---

## 3. 前端 UI 验证

### 3.1 构建验证

```
✓ vite build 成功，无错误
  dist/index.html        0.50 KB
  dist/assets/index.css  32.45 KB (gzip: 6.19 KB)
  dist/assets/index.js   273.90 KB (gzip: 84.07 KB)
```

### 3.2 TypeScript 类型检查

```
✓ tsc --noEmit 通过
  仅 1 个 tsconfig 弃用警告（baseUrl 在 TS 7.0 将不再支持）
  无类型错误
```

### 3.3 页面与组件验证

| 页面/组件            | 状态 | 验证内容                                                |
|---------------------|------|--------------------------------------------------------|
| LoginPage           | ✅    | 首次设置/登录双模式、表单验证、错误处理、Token 存储      |
| DashboardPage       | ✅    | 项目列表、创建/删除项目、导航到 AI 配置和项目详情         |
| AIConfigPage        | ✅    | 配置 CRUD、连接测试、Provider 状态显示                   |
| ProjectDetailPage   | ✅    | 大纲编辑/确认、生成控制、章节列表、知识库、日志面板       |
| API 集成 (api.ts)   | ✅    | 所有后端端点正确映射、Bearer Token 注入、SSE 流处理       |
| 状态管理 (stores)    | ✅    | Zustand stores: auth / aiConfig / generation 正确       |
| UI 组件             | ✅    | Button / Card / Input / Toast 基础组件完整               |

### 3.4 前端架构

- 路由方式：手动 state-based 路由（非 react-router）
- 状态管理：Zustand
- 样式：TailwindCSS v4
- 设计主题：「末世生存者终端」暗色游戏风格

---

## 4. 集成测试

### 4.1 测试套件（test_integration.py）

共 **17** 个端到端流程测试，模拟完整用户操作。

| 流程    | 测试内容                                     | 结果 |
|---------|---------------------------------------------|------|
| Flow 1  | 管理员注册 → 登录 → 身份验证                  | ✅    |
| Flow 1  | 配置 AI 服务（主力 + 备份）                    | ✅    |
| Flow 1  | 创建项目 → 验证初始状态                       | ✅    |
| Flow 1  | 设置大纲 + 世界观 → 确认大纲 → 状态变更       | ✅    |
| Flow 1  | 无章节时导出拒绝 (400)                        | ✅    |
| Flow 1  | 更新项目标题（大纲保留）                       | ✅    |
| Flow 2  | 创建 3 个额外项目 → 验证列表数量               | ✅    |
| Flow 2  | 删除项目 → 验证移除                           | ✅    |
| Flow 3  | 更新 AI 配置参数（temperature / max_tokens）   | ✅    |
| Flow 3  | 删除备份 AI 配置                              | ✅    |
| Flow 4  | 所有受保护端点拒绝未认证请求 (403)              | ✅    |
| Flow 4  | 无效 Token 被拒绝 (401)                       | ✅    |
| Flow 4  | 不存在资源返回 404                             | ✅    |
| Flow 5  | 知识库初始为空                                | ✅    |
| Cleanup | 登出                                         | ✅    |

**结果: 17/17 全部通过 ✅**

---

## 5. 安全性检查

| 检查项                           | 状态 | 说明                                                |
|---------------------------------|------|-----------------------------------------------------|
| JWT 认证保护                     | ✅    | 所有业务端点需 Bearer Token                          |
| 密码哈希存储                     | ✅    | passlib bcrypt 哈希，不保存明文                       |
| API Key 加密存储                 | ✅    | Fernet 对称加密，数据库中不保存明文                    |
| SQL 注入防护                     | ✅    | SQLAlchemy ORM 参数化查询                            |
| 输入校验                         | ✅    | Pydantic schema 校验 + 字段约束 (min_length 等)      |
| CORS 配置                       | ✅    | 开发模式 allow_origins=["*"]，生产环境需收窄          |
| 未认证请求拒绝                   | ✅    | 返回 403/401，经集成测试验证                          |
| 资源所有权检查                   | ✅    | 所有资源查询包含 `user_id == current_user.id` 条件    |

### 生产环境待配置项

- [ ] 设置 `JWT_SECRET` 环境变量（当前使用不安全默认值）
- [ ] 设置 `ENCRYPTION_KEY` 环境变量
- [ ] 收窄 CORS `allow_origins` 范围
- [ ] 配置 HTTPS (nginx SSL)

---

## 6. 总结

### 6.1 通过情况

| 维度         | 状态 | 说明                 |
|-------------|------|---------------------|
| API 单元测试  | ✅    | 26/26 通过           |
| 集成测试      | ✅    | 17/17 通过           |
| 前端构建      | ✅    | 无错误，无类型错误     |
| 代码评审      | ✅    | 6 个问题已修复        |
| 安全基线      | ✅    | 满足开发环境安全要求    |

### 6.2 已修复的 Bug 统计

- **严重 (Critical)**: 2 个 — JWT 密钥重启失效、项目创建 500 错误
- **高 (High)**: 4 个 — workflow state upsert、路由冲突、缺少依赖、bcrypt 兼容性
- **轻微 (Minor)**: 2 个 — 未使用的依赖包、加密密钥持久化（未修复，可接受）

### 6.3 结论

NovelGenerator Phase 1 MVP **功能验证通过**，所有核心 API 正常工作，前端代码无编译错误，完整用户工作流测试通过。项目可进入集成部署阶段。
