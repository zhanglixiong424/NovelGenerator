# 小说生成器系统设计方案（v4 修订版）

## 核心问题解决方案

| 问题 | 解决方案 |
|------|---------|
| 1. 长期记忆问题 | 小说知识库系统 + 智能摘要 + 分层 Prompt 注入 |
| 2. 关键信息错误 | 人机结合确认 + 分层一致性检查（轻量规则 + LLM 抽查） |
| 3. Token 限制 | 三层 Prompt 策略（核心/上下文/背景），逐层控制 token |
| 4. 可维护性 | 知识库版本控制 + 章节摘要持久化 |
| 5. 云端安全 | JWT 认证 + 密码哈希 + HTTPS |
| 6. 多端适配 | Electron 桌面 + Web 桌面 + Web 手机，三级特效策略 |

---

## 一、系统架构

### 1.1 整体架构

```
┌──────────────────────────────────────────────────────────────────────┐
│                         客户端层                                      │
│                                                                      │
│  ┌─────────────┐   ┌─────────────────┐   ┌─────────────────────┐   │
│  │ Electron    │   │ Web 桌面浏览器   │   │ Web 手机浏览器      │   │
│  │ 桌面应用    │   │ (Chrome/Edge/    │   │ (Android Chrome/    │   │
│  │ (.exe)      │   │  Firefox)        │   │  iOS Safari)        │   │
│  │             │   │                  │   │                     │   │
│  │ 特效: 全开  │   │ 特效: 全开       │   │ 特效: 降级          │   │
│  └──────┬──────┘   └────────┬─────────┘   └──────────┬──────────┘   │
│         │                   │                        │              │
│         └───────────────────┼────────────────────────┘              │
│                             │ HTTPS                                  │
└─────────────────────────────┼────────────────────────────────────────┘
                              │
┌─────────────────────────────┼────────────────────────────────────────┐
│                       Nginx 反向代理                                  │
│                                                                      │
│  ┌──────────────────────────┼──────────────────────────────────┐    │
│  │  静态文件 (React 构建产物)  │  /api/* → FastAPI (uvicorn)    │    │
│  │  /  → index.html            │  /api/stream/* → SSE          │    │
│  └─────────────────────────────┴───────────────────────────────┘    │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────┼────────────────────────────────────────┐
│                     后端服务层 (FastAPI)                               │
│                                                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │ 认证模块     │  │ 生成服务     │  │ AI 接入层    │              │
│  │ (JWT)        │  │ (工作流引擎) │  │ (Provider    │              │
│  │              │  │              │  │  抽象)       │              │
│  └──────────────┘  └──────┬───────┘  └──────────────┘              │
│                           │                                          │
│  ┌──────────────┐  ┌──────┴───────┐  ┌──────────────┐              │
│  │ 一致性检查   │  │ 工作流状态机 │  │ 合规检查     │              │
│  │ (轻量+重级)  │  │ (状态持久化) │  │              │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│                           │                                          │
│  ┌──────────────┐  ┌──────┴───────┐  ┌──────────────┐              │
│  │ 知识库引擎   │  │ 知识确认     │  │ 知识版本控制 │              │
│  │ (提取+摘要)  │  │ (人机结合)   │  │              │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────┼────────────────────────────────────────┐
│                     数据层 (SQLite WAL)                                │
│                                                                      │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌──────────┐ ┌────────────────┐  │
│  │ users  │ │ novels │ │chapters│ │knowledge │ │ai_provider_    │  │
│  │        │ │        │ │        │ │_base     │ │configs         │  │
│  └────────┘ └────────┘ └────────┘ └──────────┘ └────────────────┘  │
│  ┌────────────────┐ ┌──────────────────┐ ┌──────────────────────┐  │
│  │workflow_states │ │knowledge_versions│ │generation_logs       │  │
│  └────────────────┘ └──────────────────┘ └──────────────────────┘  │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端框架 | React + TypeScript | 单页应用 |
| UI 框架 | TailwindCSS + shadcn/ui | 暗黑电竞风主题定制 |
| 状态管理 | Zustand | 轻量，适合单页应用 |
| 图标 | Lucide React | 与 shadcn/ui 默认搭配，矢量可控 |
| 桌面端 | Electron + electron-builder | Windows .exe (~150MB)，内嵌 Chrome 内核 |
| 移动适配 | 响应式 Web + viewport meta | Android Chrome / iOS Safari 兼容 |
| 后端 | Python + FastAPI | 异步处理，支持 SSE 流式输出 |
| ORM | SQLAlchemy | 数据库操作 |
| 数据库 | SQLite (WAL mode) | 1-5 用户并发足够，免配置 |
| 认证 | JWT Token + bcrypt | 云端部署安全保护 |
| AI | Kimi / OpenAI / DeepSeek | Provider 抽象层，支持多模型 fallback |
| 部署 | Docker Compose + Nginx + HTTPS | 云端/自有服务器/WSL |

### 1.3 API 协议

#### 认证端点

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | /api/auth/login | 登录，返回 JWT | 否 |
| POST | /api/auth/logout | 登出 | 是 |
| GET | /api/auth/me | 获取当前用户信息 | 是 |
| POST | /api/auth/setup | 首次部署创建管理员 | 否（仅首次） |

#### 项目 CRUD

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/projects | 项目列表 |
| POST | /api/projects | 创建项目 |
| GET | /api/projects/:id | 项目详情 |
| PUT | /api/projects/:id | 更新项目 |
| DELETE | /api/projects/:id | 删除项目 |

#### 章节 CRUD

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/projects/:id/chapters | 章节列表 |
| GET | /api/chapters/:id | 章节详情 |
| PUT | /api/chapters/:id | 更新章节 |
| DELETE | /api/chapters/:id | 删除章节 |

#### 生成（SSE 流式）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/projects/:id/generate/outline | 生成大纲（SSE） |
| POST | /api/projects/:id/generate/opening | 生成开篇3章（SSE） |
| POST | /api/projects/:id/generate/batch | 批量生成（SSE） |
| POST | /api/projects/:id/generate/chapter/:no | 重新生成单章（SSE） |
| GET | /api/projects/:id/generate/status | 获取生成任务状态（断线重连用） |

SSE 事件格式：
```
event: chunk
data: {"chapter_no": 45, "text": "陈锋握紧..."}

event: knowledge_change
data: {"changes": [...], "needs_confirm": true}

event: progress
data: {"current": 45, "total": 100, "status": "generating"}

event: error
data: {"code": "AI_TIMEOUT", "message": "AI 响应超时，正在重试..."}

event: done
data: {"chapter_no": 45, "word_count": 2034}
```

#### 知识库

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/projects/:id/knowledge | 知识库概览 |
| GET | /api/projects/:id/knowledge/characters | 人物列表 |
| GET | /api/projects/:id/knowledge/characters/:cid | 人物详情 |
| PUT | /api/projects/:id/knowledge/characters/:cid | 更新人物 |
| GET | /api/projects/:id/knowledge/items | 物品列表 |
| GET | /api/projects/:id/knowledge/timeline | 时间线 |
| POST | /api/projects/:id/knowledge/confirm | 确认知识变更 |
| GET | /api/projects/:id/knowledge/versions | 版本历史 |
| POST | /api/projects/:id/knowledge/rollback/:vid | 回滚到指定版本 |

#### AI 配置

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/settings/ai | 获取 AI 配置列表 |
| POST | /api/settings/ai | 新增 AI 配置 |
| PUT | /api/settings/ai/:id | 更新 AI 配置 |
| DELETE | /api/settings/ai/:id | 删除 AI 配置 |
| POST | /api/settings/ai/:id/test | 测试 AI 连通性 |

#### 导出

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/projects/:id/export | 导出小说（TXT/Word/ePub） |

> 所有 `/api/*` 端点（除 `/api/auth/login` 和 `/api/auth/setup`）均需 `Authorization: Bearer <JWT>` 头。

### 1.4 部署架构

```yaml
# docker-compose.yml 示意
version: "3.8"
services:
  nginx:
    image: nginx:alpine
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./frontend/dist:/usr/share/nginx/html
      - ./certs:/etc/nginx/certs
    depends_on:
      - backend

  backend:
    build: ./backend
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    volumes:
      - ./data:/app/data  # SQLite 持久卷
    environment:
      - DATABASE_URL=sqlite:///./data/novel.db
      - JWT_SECRET=${JWT_SECRET}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}  # AI API Key 加密用
```

部署要点：
- **Nginx**：托管前端静态文件 + 反代后端 API
- **HTTPS**：Let's Encrypt 自动续签 或 自签证书
- **持久化**：SQLite 文件挂载到宿主机目录，避免容器重建丢数据
- **环境变量**：JWT 密钥、加密密钥通过 `.env` 文件注入，不硬编码
- **SSE 配置**：Nginx 需关闭 proxy_buffering 以支持 SSE 流式传输

### 1.5 Electron 打包策略

- **打包工具**：electron-builder，输出 Windows .exe 安装包（~150MB）
- **后端连接**：Electron 客户端连接云端后端 API（不内嵌 Python，减小包体积）
- **服务器地址配置**：首次启动提示用户输入后端服务器地址，保存到本地配置
- **自动更新**：electron-updater，从指定 URL 检查更新
- **环境检测**：`window.electronAPI` 存在 → Electron 模式，触发桌面专属功能

Electron 专属能力（Web 端不具备）：
- 自定义标题栏（frameless window + 自绘标题栏，与暗黑主题统一）
- 系统托盘（最小化到托盘，后台继续接收生成通知）
- 原生通知（批量生成完成时弹系统通知）
- 窗口大小/位置记忆
- 全局快捷键（如 Ctrl+Shift+G 快速打开）

---

## 二、小说知识库系统

### 2.1 知识库架构

```
┌─────────────────────────────────────────────────────────────┐
│                    小说知识库 (Novel KB)                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐           │
│  │  世界观      │ │  人物库      │ │  物品库      │           │
│  │  [Globe]     │ │  [Users]     │ │  [Sword]     │           │
│  │             │ │             │ │             │           │
│  │ · 世界背景  │ │ · 主角      │ │ · 武器      │           │
│  │ · 修炼体系  │ │ · 配角      │ │ · 法宝      │           │
│  │ · 势力分布  │ │ · 反派      │ │ · 丹药      │           │
│  │ · 历史事件  │ │ · 关系图谱  │ │ · 材料      │           │
│  └─────────────┘ └─────────────┘ └─────────────┘           │
│                                                              │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐           │
│  │  功法库      │ │  地图库      │ │  时间线      │           │
│  │  [Sparkles]  │ │  [Map]       │ │  [Calendar]  │           │
│  │             │ │             │ │             │           │
│  │ · 功法等级  │ │ · 场景描述  │ │ · 大事记    │           │
│  │ · 技能列表  │ │ · 地点信息  │ │ · 章节事件  │           │
│  │ · 获取方式  │ │ · 势力范围  │ │ · 人物成长  │           │
│  └─────────────┘ └─────────────┘ └─────────────┘           │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              智能检索引擎 + 摘要生成                  │   │
│  │  [Brain]                                             │   │
│  │  · 语义检索：根据章节内容自动匹配相关设定            │   │
│  │  · 动态摘要：为每个人物/物品生成当前状态摘要         │   │
│  │  · 关联推断：自动关联人物→功法→武器→事件            │   │
│  │  · 优先级排序：出场频率高的设定优先注入              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

> 方括号内为 Lucide React 图标名称，实际渲染为矢量图标。

### 2.2 知识库数据结构

```python
# 人物（带智能摘要）
class Character:
    id: UUID
    project_id: UUID
    name: str                    # 姓名
    aliases: List[str]           # 别名（JSON 数组）
    role_type: str               # 角色类型：protagonist / supporting / antagonist
    appearance: str              # 外貌描述
    personality: str             # 性格
    background: str              # 身世背景
    current_level: str           # 当前等级（如 "筑基三层"）
    skills: List[str]            # 掌握技能（JSON 数组）
    items: List[str]             # 拥有物品（JSON 数组）
    relationships: Dict[str, str] # 人物关系（JSON 对象）
    story_arc: str               # 人物故事线
    first_appearance: int        # 首次出场章节
    status: str                  # 当前状态
    is_important: bool           # 用户标记为重要
    created_at: datetime
    updated_at: datetime

    @property
    def summary(self) -> str:
        """动态摘要，用于 Prompt 注入"""
        return f"{self.name}，{self.current_level}，{self.status}"

# 物品
class Item:
    id: UUID
    project_id: UUID
    name: str
    type: str                    # weapon / artifact / consumable / material
    level: str                   # 品质等级
    description: str
    abilities: List[str]         # JSON 数组
    owner_id: UUID               # 关联 Character
    origin: str
    current_status: str
    first_appearance: int
    is_important: bool
    created_at: datetime
    updated_at: datetime

    @property
    def summary(self) -> str:
        return f"{self.name}，{self.level}，{self.current_status}"
```

### 2.3 知识提取与确认流程

```
章节生成完成
      │
      ▼
┌─────────────────────────────┐
│ 1. 自动提取知识变更草稿      │
│    · 人物等级变化            │
│    · 物品获得/消耗/转移      │
│    · 新人物出场              │
│    · 新功法获得              │
└───────────┬─────────────────┘
            │
            ▼
┌─────────────────────────────┐
│ 2. 分类处理                  │
│                             │
│  自动确认白名单:             │
│  · 数量 -1 / -2（已知消耗品）│
│                             │
│  需用户确认:                 │
│  · 等级突破                  │
│  · 获得/失去重要物品         │
│  · 新人物出场                │
│  · 人物关系变化              │
│  · 其他一切                  │
│                             │
│  信任模式（开关）:            │
│  · 打开后全部自动确认        │
│  · 适合速度优先的用户        │
└───────────┬─────────────────┘
            │
            ▼
┌─────────────────────────────┐
│ 3. 展示变更摘要              │
│                             │
│  [FileText] 第45章知识变更   │
│                             │
│  · 林凡: 炼体九层 → 筑基一层│
│  · 获得物品: 回气丹×3       │
│  · 消耗物品: 疗伤药×1       │
│                             │
│  [CheckCircle 全部确认]      │
│  [Pencil 修改]               │
└───────────┬─────────────────┘
            │
            ▼
┌─────────────────────────────┐
│ 4. 确认后更新知识库          │
│    · 保存新版本              │
│    · 更新智能摘要            │
│    · 记录变更历史            │
└─────────────────────────────┘
```

---

## 三、分层 Prompt 策略

### 3.1 Prompt 构建流程

```
生成第 N 章
      │
      ▼
┌─────────────────────────────────────────┐
│ 1. 获取基础信息                          │
│    · 章纲：第N章标题和概要               │
│    · 前文：前3章摘要（从 summary 字段读取）│
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│ 2. 第一层：核心知识（必注入）            │
│    控制在 1000 token 以内                │
│                                          │
│    · 本章出场人物（主角必含）            │
│    · 人物当前状态摘要                    │
│    · 章纲中明确提到的物品                │
│    · 章纲中明确提到的功法                │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│ 3. 第二层：相关上下文（选择性注入）      │
│    控制在 1500 token 以内                │
│                                          │
│    · 与主角关系密切的配角状态            │
│    · 近期获得的重要物品                  │
│    · 相关势力当前态度                    │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│ 4. 第三层：背景知识（按需注入）          │
│    只在需要时注入，控制在 1000 token     │
│                                          │
│    · 世界规则（如修炼体系说明）          │
│    · 历史事件（与当前情节相关）          │
│    · 特殊场景设定                        │
└───────────────┬─────────────────────────┘
                │
                ▼
        构建最终 Prompt
                │
                ▼
          AI 生成内容
```

### 3.2 Prompt 模板

```python
CHAPTER_GENERATION_PROMPT = """
你正在创作一部{genre}小说《{title}》。

=== 本章信息 ===
章节：第{chapter_no}章 {chapter_title}
章纲：{chapter_outline}

=== 前文摘要 ===
{previous_summary}

=== 关键人物状态 ===
{core_characters}

=== 相关上下文 ===
{related_context}

=== 重要提醒 ===
{important_notes}

{background_knowledge}

=== 写作要求 ===
1. 保持人物性格和说话风格一致
2. 人物等级、物品归属必须与上文一致
3. 功法威力符合设定，不可随意更改
4. 情节推进符合章纲
5. 字数约 {word_count} 字
6. 章末设置悬念

请生成本章内容。
"""
```

---

## 四、分层一致性检查

### 4.1 检查策略

#### 固定执行顺序（修复 A4）

```
章节生成完成
      │
      ▼
 一致性检查（轻量级规则匹配）
      │
      ├── 发现问题 → 标记/自动修正
      │
      ▼
 合规检查（敏感词 + 语义风险）
      │
      ├── 合规修改了内容 → 触发一次轻量一致性复检
      │
      ▼
 知识提取（自动提取 + 分类确认）
      │
      ▼
 用户确认知识变更
      │
      ▼
 更新知识库 + 保存版本
```

#### 轻量级规则检查（每章必做）

- 耗时：< 100 毫秒，零 AI 成本
- 检查项：人物等级关键词匹配、物品归属关键词匹配、功法名称关键词匹配
- 示例：知识库记录「林凡 = 筑基三层」，文中出现「林凡」和「筑基五层」→ 冲突标记

#### 重级 LLM 检查（抽查）

- 触发条件：每 10 章自动抽查 / 用户标记可疑章节 / 最终导出前全面检查
- 检查项：人物行为是否符合性格、情节逻辑是否合理、物品使用是否符合设定、时间线是否合理
- 耗时：2-3 秒/章，有 AI 成本

### 4.2 轻量级规则检查实现

```python
class LightConsistencyChecker:
    """轻量级一致性检查器"""

    def check(self, chapter_content: str, knowledge_base: KnowledgeBase) -> List[Violation]:
        violations = []

        # 检查人物等级
        for char in knowledge_base.characters.values():
            expected_level = char.current_level
            if self._has_level_mismatch(chapter_content, char.name, expected_level):
                violations.append(Violation(
                    type="level_mismatch",
                    entity=char.name,
                    expected=expected_level,
                    found=self._extract_found_level(chapter_content, char.name),
                    location=self._find_location(chapter_content, char.name)
                ))

        # 检查物品归属
        for item in knowledge_base.items.values():
            if item.owner and self._has_owner_mismatch(chapter_content, item.name, item.owner.name):
                violations.append(Violation(
                    type="owner_mismatch",
                    entity=item.name,
                    expected=item.owner.name
                ))

        return violations
```

---

## 五、知识库版本控制

### 5.1 版本控制流程

```
用户修改知识库 / 章节生成后确认知识变更
      │
      ▼
┌─────────────────────────────────────────┐
│ 1. 保存新版本                            │
│    · 记录变更内容（JSON 序列化）         │
│    · 记录变更时间                        │
│    · 记录关联章节                        │
│    · change_id: UUID 唯一标识            │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│ 2. 分析影响范围                          │
│    · 找出受影响的章节                    │
│    · 预估重新生成的 token 成本           │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│ 3. 提示用户                              │
│                                          │
│  "修改了林凡的等级，影响以下章节："      │
│  · 第45章（林凡突破章节）                │
│  · 第46-50章（涉及林凡战斗）             │
│  · 预估 token 成本：约 15,000 tokens     │
│                                          │
│  [Wrench 重新生成受影响章节]             │
│  [MapPin 仅标记，稍后处理]               │
│  [CheckSquare 选择部分章节重生成]        │
└─────────────────────────────────────────┘
```

### 5.2 原子操作保证（修复 B4）

每章是一个原子单元：**生成 → 一致性检查 → 合规检查 → 知识提取 → 用户确认 → 更新知识库**，全部完成才算成功。

- 失败的章节回滚知识库到该章开始前的状态
- 续传从最后一个成功章节的下一章开始
- 中断时不保留半成品章节

---

## 六、界面设计 — 暗黑电竞风

### 6.0 设计原则

**设计主题**：「末世生存者终端」— 为玄幻/灵异/末世题材打造的沉浸式创作界面

**核心原则**：
- 暗黑电竞风 + 年轻人审美 + 炫酷
- 所有图标使用 Lucide React 矢量图标（禁止 emoji）
- 基于 shadcn/ui 组件扩展，TailwindCSS 主题定制
- 动画统一 150-300ms，使用 ease-out 缓动
- 尊重 prefers-reduced-motion

**三级特效策略**：

| 环境 | 粒子背景 | 扫描线 | 发光效果 | 动画 |
|------|---------|--------|---------|------|
| Electron 桌面 | 全开 (150粒子) | 全开 (opacity 0.08) | 全开 (20px blur) | 全开 |
| Web 桌面浏览器 | 全开 (150粒子) | 全开 (opacity 0.08) | 全开 (20px blur) | 全开 |
| Web 手机浏览器 | 减半 (60粒子) | 禁用 | 减半 (8px blur) | 简化 |

**检测逻辑**：
- Electron：`window.electronAPI` 存在 → Electron 模式
- 桌面/手机：`matchMedia('(max-width: 767px)')` + UA 检测
- **"性能模式"开关**：任何环境下可手动关闭所有特效

### 6.1 色彩系统

```css
/* 主色调 */
--bg-primary: #0a0a0f;        /* 深邃黑 */
--bg-secondary: #12121a;      /* 次级黑 */
--bg-card: #1a1a25;           /* 卡片背景 */
--bg-hover: #252535;          /* 悬停背景 */

/* 强调色 */
--accent-orange: #ff4d00;     /* 烈焰橙 - 主要强调 */
--accent-cyan: #00d4ff;       /* 电光蓝 - 次要强调 */
--accent-purple: #b829dd;     /* 末世紫 - 特殊状态 */
--accent-green: #00ff88;      /* 毒液绿 - 成功状态 */
--accent-red: #ff0040;        /* 血红色 - 警告/错误 */

/* 强调色 RGB 三元组（用于 rgba() 透明度） */
--accent-orange-rgb: 255, 77, 0;
--accent-cyan-rgb: 0, 212, 255;
--accent-purple-rgb: 184, 41, 221;
--accent-green-rgb: 0, 255, 136;
--accent-red-rgb: 255, 0, 64;

/* 文字（D3 修复：text-muted 对比度 ≥ 4.5:1） */
--text-primary: #ffffff;      /* 纯白 */
--text-secondary: #a0a0b0;    /* 灰白（对比度 ~6.5:1） */
--text-muted: #7a7a8e;        /* 暗灰（对比度 ~4.6:1） */

/* 特效 */
--glow-orange: 0 0 20px rgba(var(--accent-orange-rgb), 0.5);
--glow-cyan: 0 0 20px rgba(var(--accent-cyan-rgb), 0.5);
--border-glow: 1px solid rgba(var(--accent-orange-rgb), 0.3);
```

### 6.2 响应式断点

| 断点 | 宽度 | 布局 |
|------|------|------|
| mobile-sm | < 375px | 极窄屏，最小化 UI |
| mobile | 375px - 767px | 单列，底部操作栏，知识库全屏 Sheet |
| tablet | 768px - 1279px | 日志折叠，知识库底部抽屉 |
| desktop | ≥ 1280px | 完整布局，知识库可固定右侧/底部抽屉 |

### 6.3 桌面端布局（≥ 1280px）

```
┌─────────────────────────────────────────────────────────────────────┐
│  TopBar                                                              │
│  [Terminal] NOVEL GENESIS v2.0    [项目选择 ▼]  [Database] [Settings]│
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  MainContent                                                         │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  项目信息条                                                  │    │
│  │  《末日进化》 | 末世异能 | 番茄小说 | 45/100章               │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐    │
│  │  章节内容区                                                  │    │
│  │  [Sword] 第45章 血战变异兽群                                 │    │
│  │                                                              │    │
│  │  废弃工厂区，警报声刺破死寂。                                │    │
│  │  陈锋握紧手中的合金战刀...                                   │    │
│  │                                                              │    │
│  │  [生成进度条 ████████████████░░░ 80%]                        │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  操作栏                                                              │
│  ◄ 第45章 ►   [Pause 暂停] [RefreshCw 重新生成] [Zap 加速]         │
│                                                                      │
│  ┌─ 系统日志 ──────────────────────────────────────────────────┐    │
│  │ [10:42:15] [CheckCircle] 第45章生成完成                      │    │
│  │ [10:42:15] [ArrowUp] 知识库更新: 陈锋 Lv.12 → Lv.13        │    │
│  │ [10:42:15] [AlertTriangle] 新知识: 获得 [变异兽核心×3]      │    │
│  │ [10:42:16] [Clock] 等待用户确认...                           │    │
│  └──────────────────────────────────────────────────────────────┘    │
│                                                                      │
├─────────────────────────────────────────────────────────────────────┤
│  BottomDrawer (知识库终端，可拖拽调整高度)                           │
│  [Database] KNOWLEDGE BASE    [Users] 12  [Sword] 28  [Sparkles] 15│
│                                                                      │
│  [展开知识库 ▲]                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 6.4 移动端布局（375px - 767px）

```
┌──────────────────────────┐
│ TopBar (简化)            │
│ [Terminal] NovelGen [Menu]│
├──────────────────────────┤
│                          │
│ MainContent              │
│ (全宽, 单列)             │
│                          │
│ · 章节内容全屏展示       │
│ · 项目信息折叠           │
│ · 系统日志折叠           │
│                          │
│ [生成进度 ████░░ 80%]    │
│                          │
├──────────────────────────┤
│ BottomActions (固定)     │
│ [Pause] [Check] [Database]│
└──────────────────────────┘

知识库 (移动端): 全屏 Sheet
┌──────────────────────────┐
│ [X] 知识库               │
│ [Search 搜索...]         │
├──────────────────────────┤
│ [Users] 生存者 12        │
│ [Sword] 装备 28          │
│ [Sparkles] 技能 15       │
│ [Package] 物资 8         │
│ [Calendar] 时间线        │
├──────────────────────────┤
│                          │
│ 列表内容...              │
│                          │
└──────────────────────────┘
手势: 下滑关闭
```

### 6.5 AI 配置页面

```
桌面端 (≥ 1280px):
┌────────────────────────────────────────────────────────┐
│  [Settings] AI PROVIDER CONFIGURATION                   │
├────────────────────────────────────────────────────────┤
│                                                        │
│  ┌── Provider #1 ──────────────────────────────────┐  │
│  │  [Zap] Kimi Pro                  [ON] 优先级: 1  │  │
│  │                                                  │  │
│  │  API Key:  [sk-***********6f3a]  [Eye]           │  │
│  │  Base URL: [https://api.moonshot.cn/v1]          │  │
│  │  Model:    [moonshot-v1-128k]                    │  │
│  │  Temp:     [0.7]  MaxTokens: [4096]              │  │
│  │                                                  │  │
│  │  状态: [CheckCircle] 连通 (2s前测试)             │  │
│  │  [PlayCircle 测试] [Pencil 编辑] [Trash2 删除]   │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  ┌── Provider #2 ──────────────────────────────────┐  │
│  │  [Bot] OpenAI GPT-4              [OFF] 优先级: 2 │  │
│  │                                                  │  │
│  │  API Key:  [sk-***********8e2b]  [Eye]           │  │
│  │  Base URL: [https://api.openai.com/v1]           │  │
│  │  Model:    [gpt-4-turbo]                         │  │
│  │                                                  │  │
│  │  状态: [XCircle] 未测试                          │  │
│  │  [PlayCircle 测试] [Pencil 编辑] [Trash2 删除]   │  │
│  └──────────────────────────────────────────────────┘  │
│                                                        │
│  [Plus 添加新配置]                                     │
│                                                        │
│  ┌── Fallback 策略 ────────────────────────────────┐  │
│  │  当前模型不可用时自动切换到下一优先级模型         │  │
│  │  最大重试次数: [3]                               │  │
│  │  重试间隔: [指数退避]                            │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘

移动端 (< 768px):
· Provider 卡片纵向堆叠，全宽
· 编辑为弹出全屏 Sheet 表单
· 触摸友好的开关 (≥44px) 和输入框
```

### 6.6 登录页面

```
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│  [粒子背景动画，低密度]                                      │
│                                                              │
│              ┌─────────────────────────┐                     │
│              │                         │                     │
│              │  [Terminal] NOVEL GENESIS│                     │
│              │                         │                     │
│              │  用户名                 │                     │
│              │  ┌───────────────────┐  │                     │
│              │  │ [User]            │  │  ← 发光边框         │
│              │  └───────────────────┘  │                     │
│              │                         │                     │
│              │  密码                   │                     │
│              │  ┌───────────────────┐  │                     │
│              │  │ [Lock]   [Eye]    │  │  ← 发光边框         │
│              │  └───────────────────┘  │                     │
│              │                         │                     │
│              │  [████ 登 录 ████]      │  ← 烈焰橙主按钮     │
│              │                         │                     │
│              └─────────────────────────┘                     │
│                                                              │
│  登录卡片: bg-card + border-glow + 微弱发光阴影              │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 6.7 组件设计规范

#### 按钮样式（基于 shadcn/ui Button 扩展）

| 类型 | 背景 | 边框 | 阴影 | 悬停效果 |
|------|------|------|------|---------|
| 主按钮（烈焰橙） | `#ff4d00` | `1px solid #ff7733` | `0 0 15px rgba(255,77,0,0.5)` | 亮度+10%, 阴影扩大 |
| 次按钮（电光蓝） | transparent | `1px solid #00d4ff` | `0 0 10px rgba(0,212,255,0.3)` | 背景 `rgba(0,212,255,0.1)` |
| 危险按钮（血红） | `#ff0040` | none | `0 0 15px rgba(255,0,64,0.5)` | 亮度+10% |

悬停动画：发光增强 + `translateY(-2px)`，时长 200ms ease-out

#### 卡片样式（基于 shadcn/ui Card 扩展）

```
背景: #1a1a25 (bg-card)
边框: 1px solid rgba(255, 77, 0, 0.2)
圆角: 8px (radius-lg)
阴影: 0 4px 20px rgba(0, 0, 0, 0.5)
顶部: 可选发光条（2px 高，渐变橙）
```

#### 进度条样式（基于 shadcn/ui Progress 扩展）

- **能量条（生成进度）**：橙红渐变 + 流动光效动画
- **经验条（等级进度）**：青色渐变 + 脉冲动画

#### z-index 分层

| 层级 | z-index | 用途 |
|------|---------|------|
| base | 0 | 普通内容 |
| card | 10 | 卡片、面板 |
| drawer | 40 | 知识库抽屉 |
| modal | 100 | 弹窗、Dialog |
| toast | 1000 | Toast 通知 |

### 6.8 动画效果规范

| 效果 | 时长 | 缓动 | 说明 |
|------|------|------|------|
| 按钮悬停 | 200ms | ease-out | 发光增强 + translateY(-2px) |
| 进度条流动 | 2s loop | linear | 光效从左到右（仅生成进行时激活） |
| 文字打字机 | 逐字 30ms | - | 光标闪烁 500ms 间隔 |
| 知识确认弹窗 | 250ms | ease-out | 从触发源 scale+fade 进入 |
| 知识库抽屉 | 300ms | ease-out | 从底部 slide-up |
| 扫描线 | 8s loop | linear | opacity 0.05-0.08，仅桌面端 |
| 粒子背景 | 持续 | - | 低速缓慢流动，不干扰阅读 |

> 所有动画尊重 `prefers-reduced-motion`：开启时禁用粒子、扫描线、流动光效，保留即时状态变化。
> 移除 glitch 动画（D4 修复），hover 效果限制为 1-2 个。

### 6.9 Electron 专属优化

| 功能 | 说明 |
|------|------|
| 自定义标题栏 | frameless window + 自绘标题栏（暗黑主题，与 TopBar 融合） |
| 系统托盘 | 最小化到托盘，后台继续接收生成通知 |
| 原生通知 | 批量生成完成时弹系统通知 |
| 窗口记忆 | 保存窗口大小和位置，下次启动恢复 |
| 全局快捷键 | Ctrl+Shift+G 快速唤起窗口 |
| 服务器配置 | 首次启动向导，输入后端服务器地址 |

---

## 七、优化后工作流程

```
用户登录
    │
    ▼
首次使用 → 配置 AI 接口（API Key、模型选择）
    │
    ▼
创建项目（书名、类型、平台）
    │
    ▼
生成大纲 + 初始化知识库
    │
    ▼
【暂停】用户确认大纲
    │
    ▼
生成开篇3章（SSE 流式输出）
    ├── 每章生成后：一致性检查 → 合规检查 → 知识提取
    ├── 展示变更摘要
    └── 用户确认后更新知识库
    │
    ▼
【暂停】用户确认开篇
    │
    ▼
批量生成（SSE 流式 + 进度推送）
    For each 章节:
      1. 分层检索相关知识（核心+上下文+背景）
      2. 构建带记忆注入的 Prompt
      3. AI 生成正文（SSE 流式输出）
      4. 一致性检查（轻量级规则匹配）
      5. 合规检查（敏感词 + 语义）
      6. 提取知识变更草稿
      7. 分类处理（自动确认白名单 / 用户确认）
      8. 确认后更新知识库 + 保存版本
      9. 生成章节摘要并存储（供后续 Prompt 使用）
      10. 每10章进行重级 LLM 检查
      11. 保存章节，推送进度
    │
    ├── SSE 断线：客户端自动重连，从断点恢复进度
    ├── 生成失败：原子回滚该章知识库状态，从失败章重试
    │
    ▼
【暂停】用户查看知识库 + 确认导出
    │
    ▼
导出小说（TXT / Word / ePub）
```

### 工作流状态机（修复 A2）

```python
class WorkflowState(Enum):
    IDLE = "idle"                    # 初始状态
    OUTLINE_GENERATING = "outline_generating"
    OUTLINE_PENDING = "outline_pending"    # 等待用户确认大纲
    OPENING_GENERATING = "opening_generating"
    OPENING_PENDING = "opening_pending"    # 等待用户确认开篇
    BATCH_GENERATING = "batch_generating"
    BATCH_PAUSED = "batch_paused"          # 用户暂停批量生成
    KNOWLEDGE_PENDING = "knowledge_pending" # 等待用户确认知识变更
    EXPORT_READY = "export_ready"
    COMPLETED = "completed"
```

持久化到 `workflow_states` 表，用户关闭浏览器再回来可恢复。

---

## 八、数据模型汇总

### 核心模型

```python
class User:
    id: UUID
    username: str                # 唯一
    password_hash: str           # bcrypt 哈希
    is_admin: bool
    created_at: datetime

class NovelProject:
    id: UUID
    user_id: UUID                # 关联 User（外键）
    title: str
    genre: str
    target_platform: str
    target_word_count: int
    status: str
    created_at: datetime
    updated_at: datetime

class Chapter:
    id: UUID
    project_id: UUID
    chapter_no: int
    title: str
    outline: str                 # 章纲
    content: str                 # 正文
    summary: str                 # 章节摘要（修复 C1）
    word_count: int
    compliance_status: str       # pass / warning / fail
    consistency_status: str      # pass / warning / fail
    status: str                  # draft / confirmed / regenerating
    created_at: datetime
    updated_at: datetime

class AIProviderConfig:
    id: UUID
    user_id: UUID                # 关联 User
    name: str                    # 显示名（如 "Kimi Pro"）
    provider_type: str           # kimi / openai / deepseek / custom
    api_key_encrypted: str       # Fernet/AES 加密存储
    base_url: str
    model_name: str
    priority: int                # 优先级（1 最高）
    is_enabled: bool
    max_tokens: int
    temperature: float
    last_test_status: str        # success / failed / untested
    last_test_at: datetime
    created_at: datetime
    updated_at: datetime
```

### 知识库模型

```python
class KnowledgeEntity:
    """知识库通用实体（人物/物品/功法/势力/地点）"""
    id: UUID
    project_id: UUID
    entity_type: str             # character / item / skill / faction / location
    name: str
    data: str                    # JSON 序列化的完整属性
    summary: str                 # 动态摘要
    is_important: bool
    first_appearance: int
    created_at: datetime
    updated_at: datetime

class KnowledgeVersion:
    """知识库版本（修复 C4：独立表）"""
    id: UUID
    project_id: UUID
    version_no: int
    chapter_no: int              # 关联章节
    created_at: datetime

class KnowledgeChange:
    """单次变更记录（修复 C2：JSON 序列化 + UUID）"""
    id: UUID                     # change_id
    version_id: UUID             # 外键关联 KnowledgeVersion
    entity_type: str
    entity_id: str               # 格式: {entity_type}:{name}:{uuid_short}
    field: str
    old_value: str               # JSON 序列化
    new_value: str               # JSON 序列化
    is_auto_extracted: bool
    is_confirmed: bool
    created_at: datetime
```

### 工作流与日志模型

```python
class WorkflowStateRecord:
    """工作流状态持久化（修复 A2）"""
    id: UUID
    project_id: UUID
    current_state: str           # WorkflowState 枚举值
    current_chapter_no: int      # 当前处理到的章节
    pending_data: str            # JSON: 待确认的知识变更等
    updated_at: datetime

class GenerationLog:
    """Prompt 历史记录（修复 C3）"""
    id: UUID
    chapter_id: UUID
    provider_id: UUID            # 使用的 AI 配置
    model_name: str
    prompt_summary: str          # 第一层+第二层知识摘要（不完整 prompt，省空间）
    input_tokens: int
    output_tokens: int
    total_tokens: int
    duration_ms: int             # 耗时
    status: str                  # success / failed / timeout
    error_message: str           # 失败原因
    created_at: datetime

class TimelineEvent:
    """时间线事件"""
    id: UUID
    project_id: UUID
    chapter_no: int
    event_type: str              # level_up / item_acquired / plot_point / ...
    description: str
    related_entities: str        # JSON 数组: 关联的实体 ID
    created_at: datetime
```

---

## 九、AI 接入层设计（修复 A3）

### Provider 抽象

```python
class AIProvider(ABC):
    """AI 提供商抽象基类"""

    @abstractmethod
    async def generate(self, prompt: str, **kwargs) -> AsyncGenerator[str, None]:
        """流式生成文本（SSE chunk）"""
        pass

    @abstractmethod
    async def test_connection(self) -> TestResult:
        """测试连通性"""
        pass

class KimiProvider(AIProvider): ...
class OpenAIProvider(AIProvider): ...
class DeepSeekProvider(AIProvider): ...
class CustomProvider(AIProvider): ...  # OpenAI 兼容接口
```

### Fallback 策略

```python
class AIService:
    """AI 服务层 - 封装重试与 fallback"""

    async def generate_with_fallback(self, prompt: str) -> AsyncGenerator[str, None]:
        providers = self.get_enabled_providers_by_priority()

        for provider in providers:
            for attempt in range(1, self.max_retries + 1):
                try:
                    async for chunk in provider.generate(prompt):
                        yield chunk
                    return  # 成功，退出
                except (Timeout, RateLimited, APIError) as e:
                    wait = min(2 ** attempt, 30)  # 指数退避，最大30秒
                    log.warning(f"{provider.name} attempt {attempt} failed: {e}, retry in {wait}s")
                    await asyncio.sleep(wait)

            log.error(f"{provider.name} exhausted retries, falling back to next provider")

        raise AllProvidersFailedError("所有 AI 提供商均不可用")
```

### 并发控制

- 批量生成时控制并发数：1-2 并发（避免触发 AI 速率限制）
- 每次调用记录 token 用量到 `generation_logs` 表
- 前端展示累计 token 消耗和预估成本

---

## 十、SSE 断线重连（修复 E4）

```
客户端 EventSource 连接
      │
      ├── 正常接收 event: chunk / progress / done
      │
      ├── 连接断开（WiFi 切换 / 锁屏 / 网络异常）
      │      │
      │      ▼
      │   自动重连（EventSource 内置 retry 字段，默认 3 秒）
      │      │
      │      ▼
      │   GET /api/projects/:id/generate/status
      │      │
      │      ├── 后端仍在生成 → 从 last_event_id 恢复推送
      │      ├── 后端生成完成 → 返回结果
      │      └── 后端生成失败 → 返回错误
      │
      └── 前端展示：
           · 正常时：隐藏
           · 断线时："连接已断开，正在重连..." (Toast)
           · 重连成功："已恢复连接" (Toast, 3s 自动消失)
```

---

## 十一、实现优先级

### Phase 0: 基础设施

- [ ] Docker Compose + Nginx 配置
- [ ] HTTPS 证书配置（Let's Encrypt）
- [ ] 数据库 Schema 创建（所有表）
- [ ] 用户认证系统（JWT + bcrypt）
- [ ] AI Provider CRUD API + 加密存储（Fernet）
- [ ] 前端骨架（Vite + React + TypeScript + TailwindCSS + shadcn/ui + Zustand + Lucide React）
- [ ] 暗黑电竞风 TailwindCSS 主题配置
- [ ] viewport meta + touch-action 配置

### Phase 1: MVP 核心

- [ ] 登录页面 UI
- [ ] AI 配置页面 UI + 连通性测试
- [ ] 项目创建与管理
- [ ] 大纲生成 + 初始化知识库（SSE）
- [ ] 开篇 3 章生成（SSE）
- [ ] **分层 Prompt 策略**（核心知识 + 相关上下文）
- [ ] **知识变更确认**（每章后展示摘要，用户一键确认 / 信任模式）
- [ ] **一致性检查 → 合规检查 → 知识提取**（固定执行顺序）
- [ ] 基础敏感词过滤
- [ ] 移动端响应式布局
- [ ] 移动端知识库全屏 Sheet
- [ ] 导出 TXT

### Phase 2: 批量生成 + Electron

- [ ] 批量生成功能 + 进度监控（SSE）
- [ ] SSE 断线自动重连 + 进度恢复
- [ ] Electron 打包（electron-builder → Windows .exe）
- [ ] Electron 自定义标题栏 + 系统托盘 + 原生通知
- [ ] **轻量级一致性检查**（规则匹配，每章自动）
- [ ] **知识库版本控制**（保存版本、查看历史、回滚）
- [ ] 知识库抽屉 UI（角色/装备/技能/物资/时间线 tab）
- [ ] **章节摘要自动生成并持久化**
- [ ] 工作流状态持久化（断点续传）
- [ ] 导出 Word

### Phase 3: 高级功能

- [ ] **重级 LLM 一致性检查**（每 10 章抽查）
- [ ] **章纲符合度检查**
- [ ] **智能摘要动态生成**
- [ ] **批量修正与重新生成**（展示影响范围 + 预估成本）
- [ ] 用户标记重要知识
- [ ] AI Provider fallback 自动切换
- [ ] Electron 自动更新（electron-updater）
- [ ] 导出 ePub
- [ ] 人物关系图谱
- [ ] 时间线可视化

---

## 十二、审核修复追踪

| ID | 类别 | 严重度 | 问题 | 修复方案 | 对应章节 |
|----|------|--------|------|---------|---------|
| A1 | 架构 | 严重 | 缺前后端通信协议 | 定义 REST + SSE API 端点清单 | 1.3 |
| A2 | 架构 | 严重 | 工作流状态无持久化 | workflow_states 表 + 状态机枚举 | 七、八 |
| A3 | 架构 | 中等 | AI 接入无重试/降级 | Provider 抽象 + 指数退避 + fallback | 九 |
| A4 | 架构 | 中等 | 检查执行顺序未定义 | 一致性→合规→知识提取（固定顺序） | 四 |
| A5 | 架构 | 低 | SQLite 并发限制 | WAL 模式 | 1.2 |
| B1 | 流程 | 严重 | 自动确认规则模糊 | 白名单 + 信任模式开关 | 2.3 |
| B2 | 流程 | 中等 | 章纲偏差无处理 | 章纲符合度检查（Phase 3） | 十一 |
| B3 | 流程 | 中等 | 级联重生成无上限 | 展示影响范围 + 预估成本 | 五 |
| B4 | 流程 | 中等 | 中断恢复不完整 | 原子操作 + 失败回滚 | 5.2 |
| C1 | 数据 | 严重 | Chapter 缺 summary | Chapter 添加 summary 字段 | 八 |
| C2 | 数据 | 中等 | KnowledgeChange 类型不安全 | JSON 序列化 + 统一 ID 格式 | 八 |
| C3 | 数据 | 低 | 缺 Prompt 历史 | generation_logs 表 | 八 |
| C4 | 数据 | 低 | 版本控制无数据库设计 | knowledge_versions + knowledge_changes 分表 | 八 |
| D1 | UI | BUG | CSS rgba 解析失败 | RGB 三元组变量 | 6.1 |
| D2 | UI | 设计 | 配色与需求不匹配 | 统一为方案色彩系统 | 6.1 |
| D3 | UI | 无障碍 | text-muted 对比度不足 | `#606070` → `#7a7a8e` (4.6:1) | 6.1 |
| D4 | UI | 交互 | Glitch 动画违规 | 移除 glitch，保留发光+微浮 | 6.8 |
| D5 | UI | 性能 | scanline 持续消耗 GPU | opacity 0.05-0.08，间歇触发 | 6.8 |
| D6 | UI | 架构 | 缺页面布局设计 | 断点+组件层级+z-index | 6.2-6.4, 6.7 |
| D7 | UI | 规范 | emoji 作图标 | 全部换 Lucide React | 全文 |
| D8 | UI | 交互 | 抽屉交互不完整 | 底部 slide-up + 可拖拽高度 | 6.3 |
| D9 | UI | 实现 | 游戏化组件无适配方案 | 基于 shadcn/ui 扩展 | 6.7 |
| E1 | 安全 | 严重 | 缺少身份认证层 | JWT + bcrypt + User 模型 | 1.3, 6.6, 八 |
| E2 | 部署 | 中等 | 部署架构未定义 | Docker Compose + Nginx + HTTPS | 1.4 |
| E3 | 文档 | 冲突 | 需求文档技术约束冲突 | 需求.md 已更新为云端部署 | （已修复） |
| E4 | 网络 | 中等 | SSE 移动端连接不稳定 | EventSource 断线重连 + 状态恢复 | 十 |
| E5 | 性能 | 中等 | 移动端特效卡顿 | 三级特效策略 | 6.0 |
| E6 | 文档 | 结构 | 2.10 编号位置错误 | 需求.md 已修复 | （已修复） |
